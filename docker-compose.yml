version: '3.8'

services:
  openclaw:
    image: alpine/openclaw:latest
    user: "1000:1000"  # Match node user ID in container
    container_name: openclaw
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config:/home/node/.openclaw
      - ./workspace:/home/node/.openclaw/workspace
    environment:
      - NODE_ENV=production
      # Optional: Set your timezone
      - TZ=Europe/Amsterdam
      # Optional: Authentication token
      # - OPENCLAW_AUTH_TOKEN=your_token_here
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '1.0'
        reservations:
          memory: 512M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    # Security options
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

# Create directories and set permissions before running:
# mkdir -p ./config ./workspace
# sudo chown -R 1000:1000 ./config ./workspace
# sudo chmod -R 755 ./config ./workspace

# Optional: Network configuration
networks:
  default:
    name: openclaw_network
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16